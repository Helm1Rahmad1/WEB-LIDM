name: Auto Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted  # ← Pakai runner di server sendiri!

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Deploy Application
        run: |
          echo "Starting deployment..."
          cd /root/lombalidm2025
          ./deploy-server.sh
