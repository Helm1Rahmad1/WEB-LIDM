NODE_ENV=production
PORT=3001

# Database Configuration
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/lidm

# Alternative format (if not using DATABASE_URL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=lidm
DB_USER=postgres
DB_PASSWORD=postgres

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Frontend URLs (comma-separated for multiple origins)
FRONTEND_URL=http://localhost:3000,https://yourdomain.com

# Email Configuration (optional, for verification emails)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-specific-password
EMAIL_FROM=SignQuran <noreply@signquran.com>

